@page
@model RazorPagesDream.Pages.DreamVMs.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DreamVM[0].DreamName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DreamVM[0].DreamText)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DreamVM[0].ReadableBy)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.DreamVM) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DreamName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DreamText)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReadableBy)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
